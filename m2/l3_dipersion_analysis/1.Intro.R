# formulas

DV ~ IV         # One factor

DV ~ IV1 + IV1  # Two factor

DV ~ IV1:IV2    # 2 - факторный со взоимодействием зависимых переменных

DV ~ IV1 + IV2 + IV1:IV2 # Main effect + interraction

DV ~ IV1 * IV2  # As previous. Main effect + interraction

DV ~ (IV1 + IV2 + IV3) ^ 2 # Эффекты плюс попарное взаимодейсвтие

DV ~ IV1 + Error(subject/IV1) # повторяемые измерения



